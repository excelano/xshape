# Values ship reads. Format and keys: anderix/ship, docs/DESIGN.md.
# winget Excelano.xshape parked: Defender flags every build and the package waits on that
targets=apt crates homebrew
apt_package=xshape
crates=xshape
